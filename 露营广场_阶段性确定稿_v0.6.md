# 露营广场｜阶段性确定稿 v0.6

> 定位：本文只记录《露营广场》v1 最终希望呈现的游戏规则、玩家体验与待定设计项。
>
> 本文不记录开发进度、提交记录、测试数量、工作区状态、施工顺序或临时技术实现。代码现状不得反向覆盖已经确认的策划规则。

---

## 1. 游戏定位与总体目标

《露营广场》是一款以营地经营、客流调度和轻量 NPC 行为为核心的经营游戏。

玩家需要管理：

- 日间营位与过夜帐篷；
- 餐饮、娱乐与绿化；
- 食材、资金、设施升级与帐篷扩建；
- NPC 的到达、消费、活动、离场与评价；
- 最终大型设施“温泉”的建设与经营。

v1 的明确终局目标是建成温泉。温泉建成后不强制结束存档，玩家仍可继续经营完全体营地。

正常频率玩家预计在现实 3～5 天内基本体验完温泉之外的主体内容；高频玩家可能在 2～3 天内推进完成，不做人为限速。温泉之外的主体内容应在终局前基本开放，避免玩家后期只剩重复攒钱。

---

## 2. 全局设计原则

### 2.1 所有机制必须是全局规则

客流、容量、NPC 行为、消费、满意度和结算规则必须适用于完整经营周期。

禁止：

- 针对某一天写特殊补丁；
- 针对某个 Turn 硬塞临时客流；
- 用固定的前期、中期、后期客流表替代真实计算；
- 因测试方便把临时实现写成最终规则。

### 2.2 需求与容量必须分离

固定顺序：

> 计算当天真实需求
> → 日初生成完整 NPC 计划包
> → 分配到达 Turn
> → 到达时检查营地能否接待

容量只能决定“能否接待”，不能反向抹掉真实需求。

客满时：

- NPC 不进入在场客组池；
- 不增加实际接待数量；
- 计划项标记为客满流失；
- 产生世界内可见事件；
- 当天不重复尝试。

### 2.3 AI 负责决策，代码负责判定

AI 或玩家负责选择经营动作；代码负责：

- 生成客流与 NPC 计划；
- 检查规则、容量、库存和余额；
- 执行扣款与收入结算；
- 更新状态；
- 记录增量事件；
- 返回下一步可执行动作。

不能让 AI 自行编造客流、收费、容量结果、预约结果或设施状态。

---

## 3. 时间与回合结构

每个游戏日共有 6 个 Turn：

- Turn 1：晨间结算；普通在住客中预定在 Turn 1 退房的客组离场；前一日已转为过夜客的客组统一在此时退房；
- Turn 2：到达窗口；普通在住客中预定在 Turn 2 退房的客组离场；
- Turn 3～4：主要到达与营业窗口；
- Turn 5：完成当日普通计划行为后进入决策前整理阶段；此时仅展示已结算完成的世界内事件结果，不再要求玩家接受或拒绝日转夜结果；
- Turn 6：AI 的日终经营决策阶段，不属于 NPC 活动轮次。

边界规则：

- Turn 2～4 为到达窗口；
- Turn 2～5 为在场 NPC 的普通活动窗口；
- 普通过夜客的退房 Turn 在其计划包中提前生成，只能是 Turn 1 或 Turn 2；
- 日转夜客不再额外随机退房 Turn，统一在次日 Turn 1 退房；
- 所有日间客无论何时到达，若未成功转为过夜客，都在 Turn 4 执行结束后、Turn 5 决策开始前完成结算并离场；
- Turn 6 由 AI 一次性提交完整的日终经营行动清单；行动数量不设上限，只受项目资格、前置状态与当前金币限制，不再拆成多个往返提交。

## 4. NPC 客组与完整计划包

### 4.1 客组单位

NPC 以“客组”为基本单位运行。

每组至少包含：

- `npc_id`；
- `group_size`；
- 日间客或过夜客类型；
- 内部来源记录；
- 三个隐藏标签；
- `arrival_turn`；
- 固定的可活动 Turn 格子；
- 普通行为计划与执行状态；
- 日转夜等独立意向事件；
- 当前满意度；
- 普通过夜客预先生成的退房 Turn。

客组人数统一为 1～6 人，六种人数等概率生成。客组人数不读取当前最大帐篷容量，也不因当日客满而提前缩小。

预约来源只作为内部账目、收费和资源锁定记录。预约客实际到访后进入与自然客相同的客组流程，不再以“预约客”身份影响当天行为概率或计划生成。

`location` 只反映客组当前正在执行或刚刚完成的行为地点。系统必须先读取计划行为，再让 NPC 移动到对应地点；不得先随机位置，再由位置反推行为。

### 4.2 三个隐藏标签

#### `spending_habit`

决定收费行为的参与概率，直接查表，不使用“基础概率 × 倍率”的多层公式。

#### `economic_level`

决定当前开放消费池中各档位被选中的相对概率，不等于必定消费。

例如三档全部开放时，高经济客组选择基础 / 中档 / 高级套餐的相对权重为 20 / 30 / 50。若高级档尚未开放，则先移除高级档，再在基础与中档之间按剩余权重重新归一化。

#### `temperament`

影响食材不足、售罄、容量不足、故障等失败后的反应文案。

v1 中 `temperament` 主要改变世界内反馈，不直接改变满意度、评分或后续决策。

### 4.3 普通行为参与判定

餐饮参与概率：

- 低消费习惯：40%；
- 普通消费习惯：55%；
- 高消费习惯：70%。

收费娱乐参与概率：

- 低消费习惯：30%；
- 普通消费习惯：50%；
- 高消费习惯：70%。

免费娱乐参与概率：

- 所有 NPC 固定 50%。

温泉建成后，温泉与餐饮、收费娱乐、免费娱乐处于同一行为层级，也由每组 NPC 独立判定是否参与。温泉参与概率仍待统一平衡。

四类普通行为分别独立 roll，允许全部未命中、只命中一项、命中任意多项或全部命中。任何一类行为都不享有排程优先级。

### 4.4 完整计划包的生成顺序

计划包按以下逻辑一次性生成：

1. 生成客组 ID 与内部来源记录；
2. 生成人数及三个隐藏标签，人数与标签之间没有顺序依赖；
3. 生成 `arrival_turn`，并据此得到固定的可活动 Turn 格子；
4. 对餐饮、收费娱乐、免费娱乐、温泉分别独立 roll；
5. 将命中的普通行为随机投放进固定的可活动 Turn 格子；
6. 在客组计划包生成时提前确定日转夜意向；
7. 若客组本来就是过夜客，再提前生成 Turn 1 或 Turn 2 的退房 Turn。

所有能够提前判定的内容都必须在当天正式 Turn 开始前完成。Turn 执行阶段不得重新 roll 当天意向、重新选择消费档位或重新安排初始普通行为。

### 4.5 三层行为生命周期

意向层：

- 判断 NPC 是否想参与某项行为；
- 只读取标签、概率和长期开放状态；
- 不读取当天剩余库存或实时容量。

计划层：

- 功能开放后，命中的普通行为才进入固定 Turn 格子；
- 保存最终档位、执行 Turn 和状态；
- 因剩余 Turn 不足而未落入格子的行为，不进入当天普通行为计划。

执行层：

- 到 `planned_turn` 时检查真实库存、容量、在场状态等动态条件；
- 成功、失败或跳过后立即结算；
- 不得回头删除或改写日初意向；
- 执行行为时才更新 `location`，位置不参与意向生成。

### 4.6 固定 Turn 格子与行为随机投放

可活动 Turn 是按时间固定排列的格子，时间轴本身不打乱。

例如 Turn 2 到达时，可活动格子固定为：

```text
Turn 2｜空
Turn 3｜空
Turn 4｜空
Turn 5｜空
```

若只命中餐饮和免费娱乐，系统会把这两个行为随机投进四个固定格子。可能得到：

```text
Turn 2｜餐饮
Turn 3｜空
Turn 4｜免费娱乐
Turn 5｜空
```

若 Turn 4 到达，可活动格子只有 Turn 4 与 Turn 5，但四类普通行为全部命中，则四个行为随机争取两个固定格子，最终只有落入格子的两个行为进入当天计划。

统一规则：

- 固定生成 `arrival_turn～Turn 5` 的时间格子；
- 随机决定哪些格子被占用；
- 随机决定哪个命中行为落入哪个已占用格子；
- 每个初始普通行为格子最多容纳一个行为；
- 行为少于格子时允许中间留空；
- 行为多于格子时，未落入格子的行为因当天时间不足而不执行；
- 餐饮、收费娱乐、免费娱乐、温泉之间没有收费优先、免费后置或其他类别优先级。

### 4.7 日转夜与其他即时事件

日转夜意向在客组计划包生成时提前确定，但它不占用普通活动 Turn 格子。

- 日初只提前判定该日间客组是否产生留宿意向；
- 触发节点为 Turn 4 执行结束后、Turn 5 决策开始前；
- 此时后台完成帐篷匹配、收费与状态变更；
- 玩家进入 Turn 5 时看到的是已经结算完成的世界内事件结果，不需要接受或拒绝。

未来 NPC 矛盾、小剧情等即时事件也可使用“计划包提前生成、到指定 Turn 自动触发”的框架，但具体事件类型、概率与影响不在 v1 当前定案范围内。

## 5. 每日需求与客流

### 5.1 经营质量 S

自然日间需求和自然过夜需求使用同一套经营质量：

```text
S = （好评率比例 + 餐饮比例 + 娱乐比例 + 绿化比例） / 4
```

其中：

- 好评率比例 = `好评率 / 100`；
- 餐饮比例 = `(餐饮等级 + 1) / 3`；
- 娱乐比例 = `(娱乐等级 + 1) / 3`；
- 绿化比例 = `当前绿化值 / 10`。

S 只读取长期经营状态，不读取当天实时容量或帐篷是否已满。

### 5.2 开发程度 D

```text
D = （日间开放比例 + 过夜开放比例） / 2
```

v1 中：

- 日间开放比例 = 1；
- 过夜开放比例 = 已解锁帐篷数量 / 6。

因此：

```text
D = （1 + 已解锁帐篷数量 / 6） / 2
```

D 只读取长期开放规模，不读取当天实时剩余容量。

### 5.3 自然需求公式

- 自然日间需求 = `S × D × 10`；
- 自然过夜需求 = `S × D × 6`。

计算结果只做一次概率取整，不先生成范围再二次随机。

### 5.4 每日需求档案

每个游戏日只生成一次完整需求档案，同一天重复读取必须复用，跨日才重新生成。

至少保存：

- 自然日间客组需求；
- 自然过夜客组需求；
- 当日是否出现预约事件；
- 预约类型与人数；
- 预约结算结果。

后续到达流程只能读取这份档案，禁止在 Turn 中重新生成当天需求。

---

## 6. 自动预约系统

### 6.1 基础规则

- 每个游戏日最多检查一次预约机会；
- 预约触发率固定为 30%；
- 未命中时，当天没有预约事件；
- 命中后，50% 为日间预约，50% 为过夜预约；
- 预约客组人数为 1～6 人等概率；
- 预约不读取 S 或 D；
- 预约属于自然需求之外的额外到访渠道。

### 6.2 自动结算

预约事件由代码自动接下或自动错失，不提供玩家手动接受 / 拒绝流程。

日间预约：

- 成功时立即收取一次日间营位费；
- 次日优先占用一个日间客组名额；
- 到达时不重复收费。

过夜预约：

- 按已解锁帐篷容量选择最小合适帐篷；
- 同容量时选择编号最小者；
- 成功时立即收取对应帐篷费用；
- 锁定次日帐篷；
- 容量不足时自动错失，不收费、不扣评价、不触发抱怨；
- 次日只使用已锁定帐篷，不临时更换；
- 暂时无法入住时保持等待，后续 Turn 重试；
- 成功入住后才清除锁定状态。

### 6.3 到访后的统一行为边界

不存在独立的“温泉专属客人”、只来泡温泉的散客渠道，也不存在预约时提前附带或锁定温泉服务。

日间预约客和过夜预约客实际到访后，与自然客使用同一套普通行为判定。温泉建成后，所有已到访客组都在计划包生成时，与餐饮和娱乐同级独立 roll 是否泡温泉。预约来源不提高、不降低温泉需求，也不提前占用温泉容量。

## 7. 日间营位与帐篷

### 7.1 日间营位

- 每日最多接待 10 组日间客；
- 只记录当天已接待组数，不建立 10 个独立营位对象；
- 开局全部开放；
- 日间客无论在哪个到达 Turn 入场，都会持续活动到 Turn 5；
- 当天不存在日间客中途离场后再次翻台；
- 成功转为过夜客也不返还已经使用过的当日日间接待名额；
- 新一天接待名额归零。

日间营位费为 **70 金币 / 组**。

该费用代表客组当天使用营地基础空间与公共设施的费用；餐饮、收费娱乐、温泉和日转夜住宿均另行收费。

### 7.2 帐篷容量

| 帐篷 | 容量 |
|---|---:|
| 1号 | 2人 |
| 2号 | 2人 |
| 3号 | 3人 |
| 4号 | 4人 |
| 5号 | 5人 |
| 6号 | 6人 |

六顶帐篷全部开放后的总过夜容量为 22 人。

帐篷必须按 2 → 3 → 4 → 5 → 6 顺序解锁。锁定帐篷不参与入住、预约、日转夜、维修、清洁、升级或容量统计。

### 7.3 住宿价格

| 帐篷 | 住宿费 |
|---|---:|
| 1号 | 160金币 / 晚 |
| 2号 | 160金币 / 晚 |
| 3号 | 230金币 / 晚 |
| 4号 | 310金币 / 晚 |
| 5号 | 400金币 / 晚 |
| 6号 | 500金币 / 晚 |

住宿费按客组收取，不按人数重复计费。预约过夜客、自然过夜客和成功日转夜客使用同一套帐篷价格。

### 7.4 2号帐篷解锁规则

2号帐篷采用“经营日门槛 + 金币”的双条件：

- 最早开放购买资格：Day 2 的 Turn 6；
- 达到资格后由玩家自行决定是否购买，系统不得自动扣款；
- 购买成功后从下一游戏日参与 D、自然需求、预约和实际接待；
- 最早可使用日为 Day 3；
- 重复购买失败且不扣款；
- 金币不足时失败，不改变状态；
- 购买价格：600金币。

未达到资格时，2号帐篷不会在普通每轮状态中主动推送，但玩家主动查询“建设 / 扩建 / 升级”列表时，应能看见锁定项目、开放条件、所需金币和当前完成进度。其他未解锁帐篷与设施遵循同一展示原则。

### 7.5 后续解锁与升级原则

3～6号帐篷、设施升级和温泉建设优先采用双条件：

> 达到经营条件 + 支付费用

经营条件负责控制内容开放节奏，价格负责形成资源取舍。不能要求单个价格同时承担“禁止过早开放”和“控制购买成本”两种职责。

3～6号帐篷按顺序开放：

| 项目 | 经营条件 | 购买价格 |
|---|---|---:|
| 3号帐篷 | 已购买2号帐篷，且累计接待15组或经营满7天 | 1100 |
| 4号帐篷 | 已购买3号帐篷，且累计接待50组或经营满12天 | 1900 |
| 5号帐篷 | 已购买4号帐篷，且累计接待90组或经营满17天 | 3200 |
| 6号帐篷 | 已购买5号帐篷，且累计接待150组或经营满23天 | 4800 |

同一 Turn 6 内，只要连续满足资格和金币条件，可以购买多个成长项目。新购帐篷从下一游戏日开始参与 D、需求、预约、入住和日转夜。

### 7.6 日转夜结算

日转夜意向在客组计划包生成时提前确定，Turn 4 执行结束后、Turn 5 决策开始前统一结算：

1. 收集所有仍在场、且命中留宿意向的日间客组；
2. 收集所有已解锁、当前空闲且状态可用的帐篷；
3. 代码进行一次整体容量匹配，优先让尽可能多的客组成功入住；
4. 每组优先分配容量最小且足够的帐篷，避免小客组无必要占用大帐篷；
5. 若多个方案在成功客组数和容量利用上完全等价，则随机决定冲突结果；
6. 成功客组另行支付对应帐篷住宿费，日间营位费不抵扣帐篷费；
7. 无合适帐篷的客组不收费，并在上述后台结算阶段正常离场；
8. 成功转为过夜客的客组统一在次日 Turn 1 退房。

整个过程由后台自动完成并一次性返回结果，不要求玩家手动接受、拒绝或安排帐篷。

## 8. 餐饮系统

### 8.1 三档套餐

| 套餐 | 价格 | 客组满意度 | 开放等级 |
|---|---:|---:|---:|
| 基础 | 30金币 / 人 | +2 | Lv0 |
| 中档 | 45金币 / 人 | +4 | Lv1 |
| 高级 | 65金币 / 人 | +6 | Lv2 |

设施等级决定开放池：

- Lv0：基础；
- Lv1：基础 + 中档；
- Lv2：基础 + 中档 + 高级。

`economic_level` 只影响开放池中各套餐被选中的相对概率：

- 低经济：基础 60 / 中档 30 / 高级 10；
- 普通经济：基础 30 / 中档 50 / 高级 20；
- 高经济：基础 20 / 中档 30 / 高级 50。

未开放档位先移出候选池，再按剩余权重重新归一化后选择。

### 8.2 食材与采购

- 全场共用整数食材库存；
- 每人消耗 1 份；
- 套餐按人数收费；
- 满意度按客组只结算一次；
- 食材不足时整组原子失败；
- 不允许部分扣库存、部分收费或部分用餐。

食材包：

- 小包：80 金币，4 份；
- 中包：150 金币，8 份；
- 大包：250 金币，14 份。

采购与跨日库存规则：

- 每个决策 Turn 的行动组合中，最多选择一次“购买食材包”；
- Turn 2～5 购买的食材立即进入当日库存，并触发仍在场等待客组的补救用餐检查；
- 不设置每日采购次数上限，玩家可以在不同 Turn 再次补货；
- Turn 5 当日全部普通行为与等待补货结算结束后，剩余当日食材统一清空，不结转到下一天；
- Turn 6 购买食材属于次日预购，进入下一游戏日的初始库存，不与已经清空的当日库存混合；
- Day 1 开局赠送8份食材，仅属于 Day 1 当日库存；
- 是否补货、购买哪种食材包，由玩家根据库存与预计需求自行判断。

### 8.3 本次到访的用餐边界

- 同一客组在本次到访期间最多成功用餐一次；
- 成功后，本次到访不再执行新的用餐；
- 客组 Turn 5 离场后，本次生命周期结束；
- 当前 v1 不自动安排同一 `npc_id` 在未来某日再次到访；
- “老客返场”属于未来扩展机制，不在当前规则内。

### 8.4 食材不足后的等待补货

原计划用餐因食材不足失败时：

1. 不扣库存、不收费、不增加满意度；
2. 该客组进入“等待补货”状态，并保留到本次到访结束；
3. 玩家未补货时，系统不在每个后续 Turn 重复进行无意义检查；
4. 玩家购买食材包导致库存增加时，代码立即重新检查仍在场的等待客组；
5. 库存足够时执行补救用餐；补救用餐可以与该客组本 Turn 原有的普通计划行为同时发生，不占用新的初始行为格子；
6. 多组同时等待而库存不足以全部满足时，按原计划用餐 Turn 从早到晚处理；
7. 未成功的客组继续等待，直到再次补货或 Turn 5 离场；
8. Turn 5 仍未成功时，本次用餐需求正式结束。

补救机制由库存变化事件触发，不按 Turn 空转轮询。

## 9. 娱乐系统

### 9.1 收费娱乐

| 档位 | 价格 | 客组满意度 | 开放等级 |
|---|---:|---:|---:|
| 基础 | 40金币 / 组 | +2 | Lv0 |
| 中档 | 60金币 / 组 | +4 | Lv1 |
| 高级 | 90金币 / 组 | +6 | Lv2 |

开放池和经济层级权重与餐饮一致。

收费娱乐按客组收费，不按人数收费。执行时直接读取日初固化的档位，不在 Turn 现场重新选择或降档。

### 9.2 免费娱乐

- 参与概率固定 50%；
- 不收费；
- 整组满意度 +1；
- 不占用收费娱乐资格；
- 时间不足时可以不生成。

免费娱乐可表现为扑克、飞盘等轻量活动。

---

## 10. 绿化系统

### 10.1 绿化等级与当前值

绿化系统必须区分：

- 绿化等级；
- 当前绿化值；
- 当前等级对应的绿化值上限；
- 当前绿化值给新入场客组带来的满意度效果。

新档：

- 绿化等级：Lv0；
- 当前绿化值：2.0；
- Lv0 上限：4.0；
- Lv1 上限：7.0；
- Lv2 上限：10.0。

对玩家统一使用“绿化值”，不使用“环境满意度”代指当前绿化状态。

### 10.2 日常维护

Turn 6 可维护绿化：

- 固定花费 50 金币；
- 当前绿化值 +1.0；
- 不超过当前等级对应的绿化值上限；
- 成功后标记当天已维护。

即使已经满值，也允许维护：

- 仍花费 50 金币；
- 当前值不再增加；
- 作用是防止次日自然衰减。

余额不足时不得扣款、不得增加绿化值、不得标记已维护。

### 10.3 自然衰减

Lv0 和 Lv1：

- 前一天未维护时，进入下一天当前绿化值 -0.5；
- 最低为 0；
- 每天最多衰减一次。

Lv2：

- 不再自然衰减；
- 不代表自动达到满值；
- 低于 10.0 时仍可花 50 金币维护，每次 +1.0，直到 10.0。

### 10.4 升级效果

每次绿化升级成功时：

- 只在升级成功的当次，将当前绿化值增加 2.0；
- 不超过升级后等级的新上限；
- 该 +2.0 绝不能每天重复；
- 升级当天视为已经完成当日维护；
- 次日不触发“上一日未维护”的衰减。

示例：

> Lv0 4/4 升至 Lv1
> → 升级当次变为 6/7
> → 不会自动变成 7/7
> → 次日不会掉到 5.5

### 10.5 客组满意度结算

客组成功进入营地时：

- 按当时的当前绿化值，为该客组增加一次满意度；
- 当前绿化值 2.5，则满意度 +2.5；
- 每组只结算一次；
- 已入场客组不因后续绿化变化被追溯修改；
- 退房时不得再次追加绿化满意度。

### 10.6 玩家可见反馈

状态展示至少包含：

- 绿化等级；
- 当前绿化值 / 当前上限；
- 今日是否维护；
- 次日是否衰减。

Lv0/Lv1 未维护时，即使当前已经满值，也必须明确提示：

> 今日尚未维护；进入下一天后绿化值将下降 0.5。

满值时维护操作仍显示，并说明：

> 当前绿化值已满，本次不会增加数值，但可防止次日衰减。

已维护后显示：

> 今日已维护，次日不会衰减。

Lv2 显示：

> 绿化状态稳定，不会自然衰减。

低于上限时仍显示可以继续维护提升。

实际衰减发生时，事件反馈为：

> 昨日未维护绿化，绿化值 X → Y。

---

## 11. 满意度、好评率与数值精度

### 11.1 连续数值允许小数

连续数值统一允许保留一位小数，包括：

- 客组总满意度；
- 好评率；
- 当前绿化值；
- 其他连续增减效果。

内部计算需规避浮点尾差，统一规范到一位小数。

显示规则：

- `62.0` 可显示为 `62`；
- `62.5` 显示为 `62.5`。

离散数量保持整数：

- 金币；
- 人数与客组数；
- 食材份数；
- 天数与 Turn；
- 设施等级；
- 最终 1～5 星评价。

星级判断使用真实满意度值比较，不先把满意度取整。

### 11.2 评价

客组离场后按既定概率留下评价。最终评价为 1～5 星整数，并影响好评率。

好评率进入经营质量 S，进一步影响后续自然需求。

具体星级阈值与好评率前期防抖方式仍需在完整平衡阶段统一确认。

---

## 12. 设施升级、维修与经济成长

### 12.1 双条件成长

帐篷解锁、餐饮升级、娱乐升级、绿化升级和温泉建设统一采用：

> 经营条件 + 金币费用

经营条件可以来自：

- 经营天数；
- 累计接待量；
- 某类服务的累计成功次数；
- 前置设施或帐篷；
- 累计完成成长节点数；
- 其他明确、可由代码验证的长期状态。

新档初始金币为 **1000金币**。

### 12.2 统一成长模型

价格和开放条件必须作为一条完整成长曲线统一计算，不逐顶帐篷、逐级设施孤立定价。

温泉之前共有 **11个成长节点**：

- 2～6号帐篷购买：5个；
- 餐饮 Lv1、Lv2：2个；
- 娱乐 Lv1、Lv2：2个；
- 绿化 Lv1、Lv2：2个。

食材采购、日常绿化维护和帐篷维修属于经营支出，不计入成长节点。

设施升级条件与费用：

| 项目 | 经营条件 | 价格 |
|---|---|---:|
| 餐饮 Lv1 | 累计成功用餐8组 | 700 |
| 餐饮 Lv2 | 已达到Lv1，累计成功用餐36组 | 1800 |
| 娱乐 Lv1 | 累计成功收费娱乐8组 | 600 |
| 娱乐 Lv2 | 已达到Lv1，累计成功收费娱乐32组 | 1600 |
| 绿化 Lv1 | 累计成功维护4次 | 600 |
| 绿化 Lv2 | 已达到Lv1，累计成功维护12次 | 1600 |

经营条件负责控制内容开放节奏，金币负责形成资源取舍。具体数值进入实机长线测试后允许整体微调，但不得用临时代码数值反向覆盖本表。

### 12.3 维修与小费

- 帐篷维修固定花费 **100金币 / 次**；
- 维修成功后帐篷恢复可用；
- 金币不足时维修失败且不改变状态；
- 小费机制仍待确认，当前基础经济不依赖小费收入。

---

## 13. 温泉终局设施

### 13.1 定位

- 温泉是独立大型终局设施；
- 不是餐饮、娱乐或绿化的普通升级等级；
- v1 只做一个温泉项目；
- 不拆档位、不做升级线；
- 建成后不强制结束存档。

### 13.2 客人使用方式

- 温泉与餐饮、收费娱乐、免费娱乐处于同一普通行为层级；
- 温泉建成后，每组已到访 NPC 在计划包生成时独立 roll 是否产生泡温泉需求；
- 命中后与其他普通行为一起随机投放到固定 Turn 格子；
- 不生成只来泡温泉的独立散客；
- 不因预约来源自动获得温泉服务，也不在预约阶段提前锁定温泉；
- 按人数收费，金额 = 单价 × `group_size`；
- 满意度按整组增加一次；
- 使用现有客组总满意度，不新增温泉独立口碑或评分系统。

### 13.3 容量

- 温泉拥有按人数计算的每日总容量；
- 所有客组共同使用同一容量；
- 执行到温泉行为时才检查当日剩余容量；
- 容量不足则整组失败，不收费、不加满意度；
- v1 不建立温泉独立预约渠道或预约容量锁定系统。

温泉当前规则：

| 参数 | 数值 |
|---|---:|
| 客组需求触发概率 | 30% |
| 每日总容量 | 20人 |
| 单价 | 80金币 / 人 |
| 满意度收益 | 整组 +6 |
| 建造费用 | 8000金币 |

温泉建设资格同时要求：

- 累计接待150组，或累计经营25天；
- 累计完成至少8个成长节点；
- 当前金币不少于8000。

达到资格后由玩家主动建设，系统不得自动扣款。建成后不强制结束存档。

## 14. 每轮输入、输出与 Token 控制

该游戏最终需要同时服务机器侧玩家与人类展示层。封装方式可以不同，但游戏内核必须先提供干净、稳定、低重复的输入输出。

### 14.1 决策提交方式

- Turn 2～5：AI 一次性提交3项行动组成的完整行动组合；
- Turn 6：AI 一次性提交完整的日终经营行动清单，行动数量不设上限；
- Turn 6 内的购买、扩建、升级、维修、维护和次日食材预购，只受各自动作资格、前置状态与金币限制；
- 同一 Turn 不拆成“先处理一个动作，再返回剩余决策点，再继续提交”的多次往返；
- 帐篷损坏、库存不足、绿化待维护等情况必须在决策前一并告知；
- 执行完成后统一返回本轮结果并进入下一 Turn。

### 14.2 默认返回原则

默认只提供会影响 AI 本轮行动选择的信息，而不是每轮发送完整经营状态。

固定保留：

- `第 X 天｜Turn X`；
- Turn 2～5 提示本轮需要提交3项行动；Turn 6 提示可提交不限数量的日终经营行动；
- 上一轮产生的关键结果；
- 本轮新出现且需要处理的情况；
- 当前可执行动作。

按当前决策需要动态附加：

- 存在付费动作时，附上该动作费用与当前可用余额；
- 补货或用餐相关时，附上当前食材库存与相关等待客组；
- 维修、入住、预约或日转夜相关时，附上必要的帐篷状态与容量；
- 绿化维护或升级相关时，附上必要的绿化摘要；
- 其他动作只附带完成判断所需的局部字段。

不应默认每轮重复发送：

- 完整余额与收入报表；
- 全部 NPC 位置；
- 完整预约历史；
- 所有设施的建设条件；
- 与当前 Turn 无关的绿化、帐篷或库存详情；
- 静态规则说明与完整历史。

未解锁帐篷、设施及其条件，只在 AI 主动查询建设 / 扩建 / 升级列表时完整展示，不主动塞入普通 Turn 返回。

### 14.3 每轮最小输出

每个 Turn 执行完成后优先返回增量信息：

- 本轮全部行动各自是否成功；
- 本轮收入与支出的实际变化；
- 关键状态变化；
- 本轮新增世界事件；
- 下一 Turn 真正需要关注的提醒；
- 下一 Turn 可执行动作。

完整状态只在明确查询、恢复上下文或必要校验时返回。

### 14.4 机器输出与人类表现分层

机器侧优先使用紧凑结构化字段；人类前端负责将相同状态渲染为地图、面板、进度条和事件日志。

不得为了人类文案，在机器侧每轮重复输出同一段解释；也不得把核心规则判断留给外部封装方自行猜测或二次计算。

## 15. 明确禁止的实现方式

- NPC 到达后才生成当天计划；
- Turn 现场重新 roll 餐饮、娱乐、温泉或消费档位；
- 默认到达 Turn 立即消费；
- 打乱 Turn 时间轴本身；
- 固定所有 NPC 先餐饮、后娱乐；
- 让收费行为优先于免费行为；
- 强制每组 NPC 至少执行一项行为；
- 人为规定每组 NPC 固定最多命中几项行为；
- 用 `location` 代替行为意愿；
- 餐饮、娱乐和温泉分别维护互不知情的排程；
- 在初始计划中让同一客组同一 Turn 占用多个普通行为格子；
- 把补货触发的用餐补救误当作新的普通行为格子；
- 为三个隐藏标签制作 27 种组合脚本；
- 使用无必要的多层概率套壳；
- 用复杂路径规划模拟 NPC 移动；
- 因容量不足而在需求生成阶段提前删减需求；
- 重新引入玩家手动接受 / 拒绝预约；
- 要求 AI 或玩家手动接受、拒绝或分配日转夜帐篷；
- 系统自动替玩家购买扩建内容；
- 把未解锁设施详情每轮主动塞给 AI；
- 把 Turn 2～5 的三项行动或 Turn 6 的完整日终行动清单拆成多次往返提交；
- 为当前小功能顺手扩建大型状态机；
- 把开发进度、提交记录、测试数量或临时工作区状态写入策划正文。

## 16. 仍待确认与统一平衡

以下内容仍未最终定案：

- 日转夜意向的最终概率，当前只确认应为20%以下的低概率特殊意向；
- 小费机制与数值；
- 主体内容对应的具体游戏内经营日数量；
- 星级阈值与好评率前期防抖方式；
- 餐饮、娱乐参与概率与经济层级权重是否需要在实机长线测试后统一微调；
- 本稿经济数值在真实引擎长线测试后的整体微调幅度。

## 17. v1 最终经营循环

```text
新一天开始
→ 结算跨天状态、退房与评价
→ 生成当天唯一需求档案
→ 生成自然日间、自然过夜与预约事件
→ 为全部客组生成完整计划包
→ 将命中的普通行为随机投放到固定 Turn 格子
→ Turn 1～5 按计划到达、消费与活动
→ 库存、容量或故障变化时由代码触发必要补救与即时结算
→ Turn 4 执行结束后至 Turn 5 决策开始前统一结算日转夜并让其余日间客离场
→ Turn 6 由 AI 一次性提交不限数量的日终经营行动清单
→ 输出精简的增量状态、事件与下一 Turn 动作
→ 进入下一天
```

玩家通过扩建帐篷、升级设施、维持绿化和积累资金逐步开放营地内容，最终建成温泉，并可继续经营完全体营地。
