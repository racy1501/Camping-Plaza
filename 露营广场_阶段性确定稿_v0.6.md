# 露营广场｜阶段性确定稿 v0.6

> 日期：2026-07-30  
> 定位：当前项目的权威阶段性策划稿。  
> 说明：本文在 v0.5 基础上同步 2026-07-30 当日开发进度。本文严格区分“已经落地”“已确认但尚未落地”“仍待平衡”，临时代码不得反向覆盖已确认设计。

---

## 1. 当前代码基线

项目路径：

`C:\Users\NewAdmin\Desktop\露营\露营广场开发\camping_plaza_fix`

当前分支：

`main`

当前阶段关键提交：

- `b4ee721 feat: add global daily visitor plans`
- `b4e11d4 feat: add planned dining actions`
- `7630b6e feat: randomize planned dining turns`
- `a7aa749 feat: add shared planned action scheduling`
- `8af3117 feat: plan daily entertainment actions`

当前工作区：

- 代码工作区已提交；
- 只剩未跟踪策划稿：
  - `露营广场_阶段性确认稿_v0.5.md`

保留旧 stash：

- `stash@{0}: On main: wip: arrival plan phase1 before review`

该 stash 暂时不得恢复、删除或修改。

---

## 2. 全局设计原则

### 2.1 所有机制必须是全局规则

所有客流、容量、NPC 行为、餐饮、娱乐和结算规则，必须适用于第 1 天至第 50 天。

禁止：

- 针对某一天写特殊补丁；
- 针对某个 Turn 硬塞临时客流；
- 用前期、中期、后期固定客流表替代真实计算；
- 因当前测试方便而把临时实现写成最终规则。

### 2.2 客流需求与容量必须分离

固定顺序：

> 全局客流公式  
> → 算出今天真实想来的全部客组  
> → 日初生成全部 NPC 计划包  
> → 分配 Turn 2～4 到达  
> → 到达时检查营地是否接得住

容量只能决定“能否接待”，不能反过来抹掉真实需求。

客满时：

- NPC 不进入 `npc_pool`；
- 不增加实际接待数量；
- 计划项标记为 `turned_away_full`；
- 产生世界内可见事件；
- 后续不再重复尝试。

### 2.3 NPC 必须使用完整计划包

NPC 在日初生成时，应一次性生成完整计划包。

所有能够提前 roll 的内容，都必须在当天 Turn 正式开始前完成，不留到 Turn 现场决定。

完整计划包至少包含：

- NPC 身份；
- 人数；
- 日间客 / 过夜客 / 预约客来源；
- 三个隐藏标签；
- `arrival_turn`；
- 餐饮参与结果；
- 收费娱乐参与结果；
- 免费娱乐参与结果；
- 消费档位；
- 行为执行 Turn；
- 行为先后顺序；
- 行为执行状态。

概念结构：

```text
NPC计划包
├─ npc_id / group_size / visit_type
├─ economic_level / spending_habit / temperament
├─ arrival_turn / arrival_status
└─ planned_actions
   ├─ dining
   │  ├─ planned_turn
   │  ├─ menu_key
   │  └─ status
   ├─ paid_entertainment
   │  ├─ planned_turn
   │  └─ status
   └─ free_entertainment
      ├─ planned_turn
      └─ status
```

这只是职责示意，不要求锁死全部字段名。

### 2.4 日初根据 `arrival_turn` 完成全部排程

日初生成 NPC 时，系统会先确定该组 NPC 的 `arrival_turn`。

之后立刻根据：

`arrival_turn ～ Turn 5`

计算该 NPC 当天可用的行动 Turn，并在日初一次性完成所有行为排程。

例如：

- 某组 NPC 在日初被确定为 Turn 4 到达；
- 系统在日初就知道该组只能使用 Turn 4、Turn 5；
- 餐饮、收费娱乐、免费娱乐的参与判定和具体执行 Turn，都在此时完成；
- Turn 4 到来后只负责让 NPC 到达，并执行早已写好的计划。

禁止将“根据可用 Turn 排程”误解为 NPC 到达后才开始安排。

### 2.5 Turn 只负责打开计划包并执行

Turn 1～5 不再重新 roll：

- 是否参加餐饮；
- 是否参加收费娱乐；
- 是否参加免费娱乐；
- 行为先后顺序；
- `planned_turn`；
- 餐饮套餐档位。

Turn 执行层只负责：

- NPC 是否已经到场；
- NPC 是否已经离开；
- 当前 Turn 是否有已排好的行为；
- 当前设施、库存和其他动态条件是否满足；
- 成功、失败或跳过的即时结算；
- 设置当前 `location`；
- 产生世界内事件。

固定逻辑：

> 日初决定想做什么、何时做  
> → Turn 到来时检查能不能做到  
> → 执行后立即结算

---

## 3. NPC 三个隐藏标签

三个标签共同进入 NPC 系统，但不要求每个标签同时进入每一条公式，也不制作 27 种组合脚本。

### 3.1 `spending_habit`

职责：

- 决定收费行为的参与概率；
- 当前已用于餐饮和收费娱乐日初判定；
- 直接查表，不再使用“基础概率 × 倍率”的多层公式。

### 3.2 `economic_level`

职责：

- 决定理想消费档位；
- 餐饮对应基础 / 中档 / 高级套餐；
- 收费娱乐后续决定消费档位或金额；
- 不直接等同于“必定消费”。

### 3.3 `temperament`

职责：

- 影响食材不足、设施不足、售罄、故障、拒绝等失败后的反应；
- 决定温和接受、普通失望或明显抱怨；
- 当前尚未正式接入生产执行链。

### 3.4 当前行为概率表

当前第一版平衡参数：

餐饮参与概率：

- 低消费习惯：40%
- 普通消费习惯：55%
- 高消费习惯：70%

收费娱乐参与概率：

- 低消费习惯：30%
- 普通消费习惯：50%
- 高消费习惯：70%

免费娱乐参与概率：

- 所有 NPC 固定 50%

这三项分别独立 roll。

允许出现：

- 三项都没有；
- 只有其中一项；
- 任意两项；
- 三项全部抽中。

这些数字是第一版平衡参数，已经进入代码，但仍可根据后续无 API 模拟结果统一调整。

---

## 4. 已实现：全局日初来客计划 Phase 1

### 4.1 每日客流只计算一次

新一天开始时顺序：

1. 结算上一天 `pending_reviews`；
2. 更新 `reputation_rate`；
3. 通过 `_calculate_daily_visitor_demand()` 计算今日自然客需求；
4. 生成今日全部自然客；
5. 建立 `today_arrival_plan`；
6. 分配 Turn 2、3、4；
7. 为每组 NPC 生成完整的日初计划；
8. 正式进入 Turn 1。

同一天不会重复计算客流，也不会因 Turn 推进重新生成一批自然客。

### 4.2 到达计划

每条计划项当前包含：

- `npc_id`
- `group_size`
- `visit_type`
- 三个隐藏标签
- `arrival_turn`
- `planned_day`
- `source`
- `arrival_status`
- `planned_actions`
- 预约相关状态

当前 `planned_actions` 已接入：

- `dining`
- `paid_entertainment`
- `free_entertainment`

### 4.3 到达与容量

- 日间客和直接过夜客先全部进入日初计划；
- 自然客到达 Turn 只会落在 Turn 2～4；
- 预约客当前固定 Turn 2 到达；
- 日间客到达时检查每日 10 组上限；
- 过夜客到达时检查当前可用、已解锁、容量足够的帐篷；
- 客满后标记 `turned_away_full`；
- 被拒客组不进入真实在场池，也不会重复尝试。

### 4.4 预约客接入

预约客已经挂入今日计划包：

- 复用预约时建立的 `npc_id`；
- 保留三个隐藏标签；
- 保留付款状态；
- 保留预约帐篷；
- 到场时不重复收取住宿费；
- 与自然客共用日初行为生成和共同排程。

---

## 5. 已实现：营位、帐篷与回合边界

### 5.1 日间营位

- 每日最多接待 10 组日间客；
- 只记录当天已接待组数；
- 不建立 10 个独立营位对象；
- 当天不翻台；
- 客人离开或转过夜不会返还名额；
- 新一天归零。

### 5.2 帐篷

容量：

- 1 号：2 人
- 2 号：2 人
- 3 号：3 人
- 4 号：3 人
- 5 号：4 人
- 6 号：5 人

解锁：

- 新游戏默认只解锁 1 号帐篷；
- 2～6 号默认锁定；
- 权威字段为 `Tent.is_unlocked`；
- 锁定帐篷不参与入住、预约、转过夜、清洁、故障、维修、升级和容量统计。

### 5.3 Turn 边界

- Turn 2～4：到达窗口；
- Turn 2～5：在场 NPC 的活动窗口；
- Turn 4 新到日间客不参与本 Turn 已经完成的转过夜判断；
- Turn 5 先处理计划行为，再让日间客统一离场；
- 日间客不进入 Turn 6；
- Turn 6 为日终管理阶段。

---

## 6. 已实现：餐饮基础规则

### 6.1 三档套餐

基础套餐：

- 30 金币 / 人
- 客组满意度 +2
- 餐饮设施 Lv0 可提供

中档套餐：

- 45 金币 / 人
- 客组满意度 +4
- 餐饮设施 Lv1 可提供

高级套餐：

- 65 金币 / 人
- 客组满意度 +6
- 餐饮设施 Lv2 可提供

餐饮设施等级决定开放套餐池：

- Lv0：`basic`
- Lv1：`basic + standard`
- Lv2：`basic + standard + premium`

`economic_level` 不再指定唯一套餐，而是只影响当前开放池内的权重：

- 低经济：`basic 60 / standard 30 / premium 10`
- 普通经济：`basic 30 / standard 50 / premium 20`
- 高经济：`basic 20 / standard 30 / premium 50`

未开放档位会先从候选池中移除，再按剩余权重选择最终套餐。

### 6.2 单一食材池

- 全场共用整数库存；
- 每人消耗 1 份；
- 套餐按人数收费；
- 满意度按客组只结算一次；
- 食材不足时整组原子失败；
- 禁止扣一半库存、收一半钱或部分用餐。

### 6.3 每日一次

- 每组 NPC 每天最多成功用餐一次；
- 权威字段为 `last_dining_day`；
- 成功后写入；
- 跨天自然恢复；
- 读档兼容旧存档缺失字段。

### 6.4 餐饮计划与执行

已实现：

- 餐饮参与判定在日初完成；
- 使用 `40% / 55% / 70%` 直接查表；
- 先按餐饮设施等级生成开放档位池；
- 再按 `economic_level` 对开放池内套餐做加权选择；
- 餐饮 action 进入 `planned_actions`；
- 餐饮 action 直接保存当日已选定的 `menu_key`；
- 餐饮与其他行为共用共同排程器；
- 餐饮不再固定等于 `arrival_turn`；
- Turn 执行只读取已保存的 `planned_turn` 与 `menu_key`；
- 成功、失败、跳过均有 action 状态；
- 同一 Turn 重复调用不会重复收费；
- 食材不足后不会跨 Turn 自动重试。

---

## 7. 已实现：共同随机排程

相关提交：

- `7630b6e feat: randomize planned dining turns`
- `a7aa749 feat: add shared planned action scheduling`

共同排程器当前规则：

1. 日初收集最终需要安排的 action；
2. 随机打乱 action，决定行为先后顺序；
3. 从 `arrival_turn～Turn 5` 中随机抽取互不重复的 Turn；
4. 将抽中的 Turn 从早到晚排序；
5. 依次分配给已经打乱的 action；
6. 最终 `planned_actions` 按 `planned_turn` 从早到晚保存。

保证：

- 行为不早于到达；
- 同一客组同一 Turn 最多一个主要行为；
- 行为 Turn 互不重复；
- 列表顺序与真实执行顺序一致；
- action 数量超过可用 Turn 数时明确报错，不偷偷重复 Turn。

---

## 8. 已实现：娱乐行为的日初计划生成 Phase 2B.1

提交：

`8af3117 feat: plan daily entertainment actions`

### 8.1 收费娱乐

已实现：

- 新游戏娱乐区默认从 Lv0 开始；
- 只有娱乐区 Lv1 及以上才开放收费娱乐项目；
- 娱乐区 Lv0 只提供扑克、飞盘等免费娱乐；
- 因此娱乐区 Lv0 在日初不进行收费娱乐参与判定；
- 娱乐区 Lv1 及以上在日初独立 roll；
- 概率直接按 `spending_habit` 查表：
  - 30%
  - 50%
  - 70%
- 抽中后生成 `paid_entertainment` action；
- 与餐饮一起进入共同排程器；
- Turn 阶段不重新 roll。

尚未实现：

- planned action 的正式执行；
- 收费金额和消费档位闭环；
- 娱乐满意度；
- 事件文案；
- 失败与跳过处理；
- 防重复结算的最终方式。

### 8.2 免费娱乐

已实现：

- 日初独立 roll 50%；
- 娱乐区 Lv0 仍可正常提供免费娱乐；
- 抽中且还有可用 Turn 时生成 `free_entertainment` action；
- 不收费；
- 不占用收费娱乐资格；
- 与其他保留行为一起进入共同排程器；
- Turn 阶段不重新 roll。

免费娱乐可对应扑克、飞盘等免费借用活动。具体效果尚未进入执行代码。

### 8.3 行为优先级与时间不足

收费行为优先：

- `dining`
- `paid_entertainment`

免费娱乐只有在抽中且仍有可用 Turn 时才加入。

例如 Turn 4 到达，三项全部抽中：

- 可用 Turn 只有 Turn 4、Turn 5；
- 保留餐饮和收费娱乐；
- 免费娱乐不生成。

这不是人为设置“每天最多两项行为”，而是该客组当天真实可用时间不足。

NPC 不存在“至少必须做一项”或“固定最多做几项”的人为配额。

---

## 9. 娱乐旧逻辑现状

旧 `_process_entertainment()` 仍然存在，并且仍是旧式逻辑：

- 遍历 `npc_pool`；
- 依赖 `location == "entertainment"`；
- Turn 中临时 roll 消费；
- 使用旧的基础概率与倍率；
- 只增加余额和娱乐收入。

当前该逻辑没有稳定的生产触发入口，但仍未删除或改造。

后续 Phase 2B.2 必须：

- 改为读取 `paid_entertainment` 和 `free_entertainment` action；
- 删除 Turn 现场参与 roll；
- 完成收费、免费效果、满意度、事件和状态闭环；
- 明确是否需要新增每日娱乐成功字段，或直接依赖 planned action 状态防重；
- 确认 `npc.location` 的执行语义。

---

## 10. 当前明确否决的实现方式

以下方向不得采用：

- NPC 到达后才生成计划；
- 到达后再 roll 当天想做什么；
- Turn 现场重新 roll 餐饮或娱乐参与意愿；
- 默认到达 Turn 立即消费；
- 固定所有 NPC 先餐饮、后娱乐；
- 强制每组 NPC 至少进行一项行为；
- 人为规定每组 NPC 固定最多进行几项行为；
- 用 `location` 代替行为意愿；
- 餐饮和娱乐分别维护互不知情的排程；
- 同一 Turn 给同一客组安排多个主要行为；
- 为三个标签制作 27 种组合脚本；
- 使用“基础概率 × 倍率”增加无意义公式层级；
- 用复杂路径规划模拟 NPC 移动；
- 因测试方便提前按容量截断需求；
- 为当前功能顺手扩建大状态机。

---

## 11. 当前开发进度

### 已完成

- Phase 1：全局日初客流与到达计划；
- Phase 2A：餐饮 planned action 与执行闭环；
- Phase 2A.1：餐饮随机 Turn；
- 共同随机排程底座；
- Phase 2B.1：收费娱乐和免费娱乐的日初计划生成。

### 下一阶段

#### Phase 2B.2：娱乐执行闭环

下一步应先做只读核对，再确认最少规则。

需要处理：

- 收费娱乐 action 的执行；
- 免费娱乐 action 的执行；
- 收费娱乐消费档位与价格；
- 娱乐满意度；
- 免费娱乐效果；
- 世界内事件；
- `completed / failed / skipped`；
- 防重复结算；
- `npc.location`；
- 移除 Turn 现场娱乐 roll。

#### 后续

- `temperament` 接入失败反应；
- 预约标签分布与自然客统一；
- 帐篷付费解锁；
- 小费与维修金币；
- 前端与 MCP 状态同步；
- 完整经济平衡。

---

## 12. 仍未定案或仍待平衡

当前仍需后续确认：

- 收费娱乐的项目档位；
- 收费娱乐最终价格；
- 收费娱乐满意度；
- 免费娱乐的具体效果；
- 免费娱乐是否影响点赞、评价或其他轻量反馈；
- `temperament` 的反应文案与是否影响数值；
- 娱乐设施升级的最终收益；
- 餐饮 `40% / 55% / 70%` 是否需要平衡调整；
- 餐饮开放池内的权重参数是否需要平衡调整；
- 收费娱乐 `30% / 50% / 70%` 是否需要平衡调整；
- 免费娱乐 50% 是否需要平衡调整；
- 自然客基础概率与声望修正公式；
- 直接过夜客最终需求公式；
- 预约触发概率与日期规则；
- 帐篷解锁价格；
- 食材补货包最终价格与份数；
- 开局金币；
- 小费概率与金额；
- 成就阈值与趣味物品。

概率调整应集中修改配置表，并通过本地无 API 模拟观察：

- 平均每组 NPC 行为数量；
- 完全没有计划行为的客组比例；
- 免费娱乐被晚到客组挤掉的比例；
- 餐饮与娱乐收入；
- 三档消费习惯的差异；
- 行为是否过度同质化。

---

## 13. 测试与技术状态

当前专项测试：

- `tests.test_entertainment_planned_actions_phase2b`
  - 13 条通过
- `tests.test_dining_planned_actions_phase2a`
  - 16 条通过
- `tests.test_arrival_plan_phase1`
  - 5 条通过

已确认：

- 三项参与判定独立 roll；
- 所有新增 roll 只发生在日初；
- Turn 3 到达且三项全中时，三项可排入 Turn 3～5；
- Turn 4 到达且三项全中时，保留两项收费行为；
- 同一天重复确保计划时不重新 roll；
- 预约客和自然客共用计划生成链；
- 新测试未发现编码污染。

已知环境问题：

- Windows 下 `TemporaryDirectory.cleanup()` 偶发 `PermissionError [WinError 5]`；
- 该问题发生在测试清理阶段，不属于业务断言失败；
- 暂不在 NPC 行为开发中顺手处理。

---

## 14. 本版结论

`v0.6` 已将 NPC 行为系统推进到：

> 日初生成全部 NPC  
> → 日初确定 `arrival_turn`  
> → 日初独立判定餐饮、收费娱乐、免费娱乐  
> → 根据 `arrival_turn～Turn 5` 的预先可知范围完成共同排程  
> → 将完整结果写入计划包  
> → Turn 只读取并执行

当前已经完成“NPC 今天想做什么、什么时候做”的计划层。

下一步只处理“到了对应 Turn 后，娱乐行为如何正确执行和结算”。

后续不得重新引入到达后排程、Turn 现场 roll、固定行为配额或多层概率套壳。
