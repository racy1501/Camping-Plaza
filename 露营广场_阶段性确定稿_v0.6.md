# 露营广场｜阶段性确定稿 v0.6

> 日期：2026-07-30  
> 定位：当前项目的权威阶段性策划稿。  
> 说明：本文在 v0.5 基础上同步 2026-07-30 当日开发进度。本文严格区分“已经落地”“已确认但尚未落地”“仍待平衡”，临时代码不得反向覆盖已确认设计。

---

## 1. 当前代码基线

项目路径：

`C:\Users\NewAdmin\Desktop\露营\露营广场开发\camping_plaza_fix`

当前分支：

`main`

当前阶段关键提交：

- `b4ee721 feat: add global daily visitor plans`
- `b4e11d4 feat: add planned dining actions`
- `7630b6e feat: randomize planned dining turns`
- `a7aa749 feat: add shared planned action scheduling`
- `8af3117 feat: plan daily entertainment actions`

当前工作区：

- 代码工作区已提交；
- 只剩未跟踪策划稿：
  - `露营广场_阶段性确认稿_v0.5.md`

保留旧 stash：

- `stash@{0}: On main: wip: arrival plan phase1 before review`

该 stash 暂时不得恢复、删除或修改。

---

## 2. 全局设计原则

### 2.1 所有机制必须是全局规则

所有客流、容量、NPC 行为、餐饮、娱乐和结算规则，必须适用于第 1 天至第 50 天。

禁止：

- 针对某一天写特殊补丁；
- 针对某个 Turn 硬塞临时客流；
- 用前期、中期、后期固定客流表替代真实计算；
- 因当前测试方便而把临时实现写成最终规则。

### 2.2 客流需求与容量必须分离

固定顺序：

> 全局客流公式  
> → 算出今天真实想来的全部客组  
> → 日初生成全部 NPC 计划包  
> → 分配 Turn 2～4 到达  
> → 到达时检查营地是否接得住

容量只能决定“能否接待”，不能反过来抹掉真实需求。

客满时：

- NPC 不进入 `npc_pool`；
- 不增加实际接待数量；
- 计划项标记为 `turned_away_full`；
- 产生世界内可见事件；
- 后续不再重复尝试。

### 2.3 NPC 必须使用完整计划包

NPC 在日初生成时，应一次性生成完整计划包。

所有能够提前 roll 的内容，都必须在当天 Turn 正式开始前完成，不留到 Turn 现场决定。

完整计划包至少包含：

- NPC 身份；
- 人数；
- 日间客 / 过夜客 / 预约客来源；
- 三个隐藏标签；
- `arrival_turn`；
- 餐饮参与结果；
- 收费娱乐参与结果；
- 免费娱乐参与结果；
- 消费档位；
- 行为执行 Turn；
- 行为先后顺序；
- 行为执行状态。

概念结构：

```text
NPC计划包
├─ npc_id / group_size / visit_type
├─ economic_level / spending_habit / temperament
├─ arrival_turn / arrival_status
└─ planned_actions
   ├─ dining
   │  ├─ planned_turn
   │  ├─ menu_key
   │  └─ status
   ├─ paid_entertainment
   │  ├─ planned_turn
   │  └─ status
   └─ free_entertainment
      ├─ planned_turn
      └─ status
```

这只是职责示意，不要求锁死全部字段名。

### 2.4 日初根据 `arrival_turn` 完成全部排程

日初生成 NPC 时，系统会先确定该组 NPC 的 `arrival_turn`。

之后立刻根据：

`arrival_turn ～ Turn 5`

计算该 NPC 当天可用的行动 Turn，并在日初一次性完成所有行为排程。

例如：

- 某组 NPC 在日初被确定为 Turn 4 到达；
- 系统在日初就知道该组只能使用 Turn 4、Turn 5；
- 餐饮、收费娱乐、免费娱乐的参与判定和具体执行 Turn，都在此时完成；
- Turn 4 到来后只负责让 NPC 到达，并执行早已写好的计划。

禁止将“根据可用 Turn 排程”误解为 NPC 到达后才开始安排。

### 2.5 Turn 只负责打开计划包并执行

Turn 1～5 不再重新 roll：

- 是否参加餐饮；
- 是否参加收费娱乐；
- 是否参加免费娱乐；
- 行为先后顺序；
- `planned_turn`；
- 餐饮套餐档位。

Turn 执行层只负责：

- NPC 是否已经到场；
- NPC 是否已经离开；
- 当前 Turn 是否有已排好的行为；
- 当前设施、库存和其他动态条件是否满足；
- 成功、失败或跳过的即时结算；
- 设置当前 `location`；
- 产生世界内事件。

固定逻辑：

> 日初决定想做什么、何时做  
> → Turn 到来时检查能不能做到  
> → 执行后立即结算

### 2.6 NPC 行为三层生命周期

所有 NPC 行为统一分为三层：

意向层：

- 判断 NPC 是否想参与某项行为；
- 只由 NPC 标签、行为概率和需求规则决定；
- 不读取动态库存、剩余容量或当天已使用数量。

计划层：

- 只有对应设施或功能已经开放，意向才写入 `planned_actions`；
- 设施未建成时，代码可以跳过无实际用途的 roll，但业务语义仍是“当前没有可执行服务”，不是“NPC 因资源不足而没有意向”。

执行层：

- 到 `planned_turn` 时才检查真实动态资源；
- 食材不足、温泉容量已满、客人已离场等，只影响执行成功或失败；
- 不得回头删除或重写日初已经生成的意向。

预约属于同一套规则中的“提前锁定”：

- 预约事件触发时立即付款；
- 预约资源提前锁定；
- 次日对应 action 直接进入计划，不再 roll；
- 执行时消费已锁定资源，不重复收费。

两个简短例子：

- 餐饮：日初生成 dining action；执行时检查食材；食材不足则 failed，不收费、不加满意度。
- 温泉：日初生成 hot_spring action，不看当天剩余名额；执行时检查温泉总容量 - 已使用人数 - 尚未执行的预约锁定人数；容量足够则收费并增加整组满意度；容量不足则整组 failed，不收费、不加满意度。

补充约束：

- 预约客和普通客共同使用温泉每日总容量；
- 预约名额优先保护；
- 普通客执行时不能占用尚未执行的预约锁定名额；
- v1 行为均按整组执行，不拆分组内个人。

---

## 3. NPC ?????? NPC 三个隐藏标签

三个标签共同进入 NPC 系统，但不要求每个标签同时进入每一条公式，也不制作 27 种组合脚本。

### 3.1 `spending_habit`

职责：

- 决定收费行为的参与概率；
- 当前已用于餐饮和收费娱乐日初判定；
- 直接查表，不再使用“基础概率 × 倍率”的多层公式。

### 3.2 `economic_level`

职责：

- 决定理想消费档位；
- 餐饮对应基础 / 中档 / 高级套餐；
- 收费娱乐后续决定消费档位或金额；
- 不直接等同于“必定消费”。

### 3.3 `temperament`

职责：

- 影响食材不足、设施不足、售罄、故障、拒绝等失败后的反应；
- 决定温和接受、普通失望或明显抱怨；
- 已最小接入餐饮食材不足场景；
- 当前只改变事件反馈文案；
- 不影响数值、评分或后续决策；
- 其他失败场景以后按需要逐步接入。

### 3.4 当前行为概率表

当前第一版平衡参数：

餐饮参与概率：

- 低消费习惯：40%
- 普通消费习惯：55%
- 高消费习惯：70%

收费娱乐参与概率：

- 低消费习惯：30%
- 普通消费习惯：50%
- 高消费习惯：70%

免费娱乐参与概率：

- 所有 NPC 固定 50%

这三项分别独立 roll。

允许出现：

- 三项都没有；
- 只有其中一项；
- 任意两项；
- 三项全部抽中。

这些数字是第一版平衡参数，已经进入代码，但仍可根据后续无 API 模拟结果统一调整。

### 3.5 经营质量 S

全局需求、预约与营地开发评估使用同一套经营质量 `S`。

```text
S = （好评率比例 + 餐饮比例 + 娱乐比例 + 绿化比例） / 4
```

其中：

- 好评率比例 = `reputation_rate / 100`
- 餐饮比例 = `(餐饮等级 + 1) / 3`
- 娱乐比例 = `(娱乐等级 + 1) / 3`
- 绿化比例 = `greenery_satisfaction / 10`

`S` 只来自长期经营状态，不读取当天容量、不读取帐篷是否已满。

### 3.6 营地开发程度 D

全局自然需求还会乘以营地开发程度 `D`。

```text
D = （日间开放比例 + 过夜开放比例） / 2
```

当前版本：

- 日间开放比例 = `1`
- 过夜开放比例 = `已解锁帐篷数量 / 6`

因此：

```text
D = （1 + 已解锁帐篷数量 / 6） / 2
```

`D` 只读长期开放规模，不读取当天剩余容量、不读取实时满额情况。

---

## 4. 已实现：全局日初来客计划 Phase 1

### 4.1 统一日需求入口 `daily_demand_profile`

每个游戏日只生成一次 `daily_demand_profile`，并保存当天需求结果。

第一版至少包含：

- `natural_day_group_demand`
- `natural_overnight_group_demand`
- `reservation_request_available`

这三项都属于需求层，只表示今天有多少自然客想来，以及今天是否出现一条面向次日的预约事件。

它们共同遵守：

- 只使用同一份当天经营状况作为上游输入；
- 分别独立生成，不互相扣减；
- 不读取当天剩余容量；
- 不读取帐篷是否已满；
- 不因当前开放帐篷数量减少过夜需求；
- 客组人数生成也不得读取当前最大已解锁帐篷容量；
- 不读取当前剩余可接待人数；
- 具体概率、数量公式和客组人数分布已经确认，不再由后续 Turn 重新 roll。

当前版本的自然需求计算：

- 自然日间需求 = `S × D × 10`
- 自然过夜需求 = `S × D × 6`

以上数值只做一次概率取整，不先生成范围再二次随机。

预约事件也由同一份 `daily_demand_profile` 继续分流：

- 第一轮 roll：`30%` 触发预约事件，`70%` 当天没有预约事件；
- 命中后第二轮 roll：`50%` 日间预约，`50%` 过夜预约；
- 客组人数：`1～6`，六种人数等概率；
- 所有预约客组独立随机一次；
- 自然日间客、自然过夜客、日间预约客、过夜预约客全部使用同一套客组人数规则；
- 不读取当前最大已解锁帐篷容量；
- 不读取当前是否客满；
- 不读取当前空闲帐篷数量。

后续流程只能读取这个 profile：

- `today_arrival_plan` 读取 `natural_day_group_demand` 和 `natural_overnight_group_demand`；
- 预约事件生成读取 `reservation_request_available`；
- 不得在其他函数或 Turn 中再次 roll 当天需求。

容量判定继续留在后续阶段：

- 自然客实际到达时判断是否有位置；
- 预约事件生成时只决定是否触发，不提前按容量裁掉需求；
- 预约确认与次日到达时判断能否锁定资源；
- 需求生成不得提前因容量不足而减少、取消或改写需求。

当前代码技术债仍保留为旧实现，后续接入 `daily_demand_profile` 时统一替换：

- 现有自然过夜需求仍按解锁帐篷逐顶 roll；
- 现有过夜客组人数上限仍受已解锁帐篷最大容量影响；
- 现有预约请求仍使用独立固定 30%。

### 4.2 到达计划

每条计划项当前包含：

- `npc_id`
- `group_size`
- `visit_type`
- 三个隐藏标签
- `arrival_turn`
- `planned_day`
- `source`
- `arrival_status`
- `planned_actions`
- 预约相关状态

当前 `planned_actions` 已接入：

- `dining`
- `paid_entertainment`
- `free_entertainment`

### 4.3 到达与容量

- 自然来客与预约请求都进入同一套 `today_arrival_plan`；
- `today_arrival_plan` 只读取 `daily_demand_profile` 里的自然日间与自然过夜需求；
- 预约事件的出现与否读取 `daily_demand_profile.reservation_request_available`，并在后续作为额外到访并入；
- 容量只决定“能否接待”，不回头抹掉真实需求；
- `turned_away_full` 只表示当前无法接待，不代表当天没有需求。

### 4.4 预约客接入

- 预约事件由本地代码自动生成并自动结算，不增加玩家操作、不增加 MCP 调用、不增加模型调用或 token 消耗；
- 每个游戏日最多检查一次预约机会；
- 每天最多出现一条预约事件；
- 预约事件触发后立即写入预约记录，并保存 `visit_type`、`group_size`、`arrival_day`、隐藏标签、预约状态与预约资源；
- 日间预约自动占用次日一个日间名额；
- 过夜预约自动占用次日对应帐篷；
- 预约客次日到达时直接进入 `today_arrival_plan`；
- 已确认的预约资源在次日到达前持续锁定；
- 预约资源的占用优先于普通客的同类资源。

### 4.5 预约总则

- 预约不再要求玩家点击接受或拒绝；
- 预约属于每日经营中的轻量随机事件，由本地代码自动生成并自动结算；
- 预约事件只在日初或日内既定节点检查一次；
- 每个游戏日最多出现一条预约事件；
- 预约客只有日间营位预约与过夜帐篷预约两类；
- 不存在第三类“温泉专属客人”；
- 不做只来泡温泉、不要营位或帐篷的温泉散客；
- 温泉只是日间客或过夜客计划包中的可选附加服务；
- 预约事件不会额外增加玩家操作，也不会引入新的状态机。

### 4.6 预约与自然需求关系

- 自然来客与预约事件共用同一套上游经营数据和 `daily_demand_profile`；
- `natural_day_group_demand`、`natural_overnight_group_demand`、`reservation_request_available` 都由同一份 profile 提供；
- 共用的是经营数据与需求强度，不是共用同一次随机结果；
- 自然来客属于即时到访渠道；
- 预约事件属于提前规划的未来到访渠道；
- 预约事件不能继续使用独立固定 30% 的旧口径；
- 不允许日间预约、帐篷预约、温泉预约各自单独 roll；
- 正确流程是：每天先生成一次 `daily_demand_profile`，再由它分流自然需求与预约事件，命中后再决定日间或过夜类型，温泉建成后再决定是否附带整组温泉预约；
- 次日到达时，日间预约优先占用日间名额，过夜预约优先占用已预留帐篷，自然客随后按正常到达流程尝试接待；
- 当预约客次日到达时，当日实际到达需求 = 当日自然需求 + 已确认预约；
- 预约客不占用自然来客需求数量；
- 容量不参与自然需求生成，只负责预约能否接受、资源是否锁定和实际到达能否接待；
- 预约资源优先，自然客容量不足时可以被拒绝；
- 预约属于同一套行为生命周期中的提前锁定，不是另一套独立规则。

---

## 5. 已实现：营位、帐篷与回合边界

### 5.1 日间营位

- 每日最多接待 10 组日间客；
- 只记录当天已接待组数；
- 不建立 10 个独立营位对象；
- 10 个日间营位从开局全部开放，不新增日间营位扩建系统；
- 当天不翻台；
- 客人离开或转过夜不会返还名额；
- 新一天归零。

### 5.2 帐篷解锁

- 1 号帐篷容量为 2 人，默认解锁；
- 2 号帐篷容量为 2 人；
- 3 号帐篷容量为 3 人；
- 4 号帐篷容量为 4 人；
- 5 号帐篷容量为 5 人；
- 6 号帐篷容量为 6 人；
- 六顶帐篷全部开放后的过夜总容量为 22 人；
- 帐篷必须按 2 → 3 → 4 → 5 → 6 顺序解锁；
- 解锁属于 Turn 6 日终管理行为；
- 重复解锁失败且不扣金币；
- 金币不足时失败，不改变状态；
- `Tent.is_unlocked` 为当前权威状态；
- 锁定帐篷当前排除于入住、预约、转过夜、维修、清洁、升级和容量统计；
- 帐篷解锁价格尚未最终确定，当前测算结果仅作参考，不写成正式价格。

### 5.3 Turn 边界

- Turn 2～4：到达窗口；
- Turn 2～5：在场 NPC 的活动窗口；
- Turn 4 新到日间客不参与本 Turn 已经完成的转过夜判断；
- Turn 5 先处理计划行为，再让日间客统一离场；
- 日间客不进入 Turn 6；
- Turn 6 为日终管理阶段。

---

## 6. 已实现：餐饮基础规则

### 6.1 三档套餐

基础套餐：

- 30 金币 / 人
- 客组满意度 +2
- 餐饮设施 Lv0 可提供

中档套餐：

- 45 金币 / 人
- 客组满意度 +4
- 餐饮设施 Lv1 可提供

高级套餐：

- 65 金币 / 人
- 客组满意度 +6
- 餐饮设施 Lv2 可提供

餐饮设施等级决定开放套餐池：

- Lv0：`basic`
- Lv1：`basic + standard`
- Lv2：`basic + standard + premium`

`economic_level` 不再指定唯一套餐，而是只影响当前开放池内的权重：

- 低经济：`basic 60 / standard 30 / premium 10`
- 普通经济：`basic 30 / standard 50 / premium 20`
- 高经济：`basic 20 / standard 30 / premium 50`

未开放档位会先从候选池中移除，再按剩余权重选择最终套餐。

### 6.2 单一食材池

- 全场共用整数库存；
- 每人消耗 1 份；
- 套餐按人数收费；
- 满意度按客组只结算一次；
- 食材不足时整组原子失败；
- 禁止扣一半库存、收一半钱或部分用餐。

### 6.3 每日一次

- 每组 NPC 每天最多成功用餐一次；
- 权威字段为 `last_dining_day`；
- 成功后写入；
- 跨天自然恢复；
- 读档兼容旧存档缺失字段。

### 6.4 餐饮计划与执行

已实现：

- 餐饮参与判定在日初完成；
- 使用 `40% / 55% / 70%` 直接查表；
- 先按餐饮设施等级生成开放档位池；
- 再按 `economic_level` 对开放池内套餐做加权选择；
- 餐饮 action 进入 `planned_actions`；
- 餐饮 action 直接保存当日已选定的 `menu_key`；
- 餐饮与其他行为共用共同排程器；
- 餐饮不再固定等于 `arrival_turn`；
- Turn 执行只读取已保存的 `planned_turn` 与 `menu_key`；
- 成功、失败、跳过均有 action 状态；
- 同一 Turn 重复调用不会重复收费；
- 食材不足后不会跨 Turn 自动重试。

---

## 7. 已实现：共同随机排程

相关提交：

- `7630b6e feat: randomize planned dining turns`
- `a7aa749 feat: add shared planned action scheduling`

共同排程器当前规则：

1. 日初收集最终需要安排的 action；
2. 随机打乱 action，决定行为先后顺序；
3. 从 `arrival_turn～Turn 5` 中随机抽取互不重复的 Turn；
4. 将抽中的 Turn 从早到晚排序；
5. 依次分配给已经打乱的 action；
6. 最终 `planned_actions` 按 `planned_turn` 从早到晚保存。

保证：

- 行为不早于到达；
- 同一客组同一 Turn 最多一个主要行为；
- 行为 Turn 互不重复；
- 列表顺序与真实执行顺序一致；
- action 数量超过可用 Turn 数时明确报错，不偷偷重复 Turn。

---

## 8. 已实现：娱乐行为的日初计划生成 Phase 2B.1

提交：

`8af3117 feat: plan daily entertainment actions`

### 8.1 收费娱乐

已实现：

- 新游戏娱乐区默认从 Lv0 开始；
- 娱乐区设施等级决定收费档位开放池：
  - Lv0：`basic`
  - Lv1：`basic + standard`
  - Lv2：`basic + standard + premium`
- 所有设施等级都在日初独立 roll 收费娱乐参与；
- 概率直接按 `spending_habit` 查表：
  - 30%
  - 50%
  - 70%
- 抽中后复用餐饮的 `_choose_weighted_unlocked_tier_key(...)`；
- `economic_level` 只影响开放池内权重，不直接指定唯一档位：
  - 低经济：`basic 60 / standard 30 / premium 10`
  - 普通经济：`basic 30 / standard 50 / premium 20`
  - 高经济：`basic 20 / standard 30 / premium 50`
- 抽中后生成 `paid_entertainment` action，并保存最终 `tier_key`；
- 收费娱乐按整组收费：
  - `basic`：30 金币 / 组，满意度 +2
  - `standard`：45 金币 / 组，满意度 +4
  - `premium`：65 金币 / 组，满意度 +6
- 与餐饮一起进入共同排程器；
- Turn 阶段不重新 roll，也不重新选档或降档。

当前概率和权重表都属于待本地模拟验证的第一版参数。

### 8.2 免费娱乐

已实现：

- 日初独立 roll 50%；
- 所有娱乐设施等级都可提供免费娱乐；
- 抽中且还有可用 Turn 时生成 `free_entertainment` action；
- 不收费；
- 收入 0；
- 整组满意度 +1；
- 不占用收费娱乐资格；
- 与其他保留行为一起进入共同排程器；
- Turn 阶段不重新 roll。

免费娱乐可对应扑克、飞盘等免费借用活动，当前已进入执行代码。

### 8.3 行为优先级与时间不足

收费行为优先：

- `dining`
- `paid_entertainment`

免费娱乐只有在抽中且仍有可用 Turn 时才加入。

例如 Turn 4 到达，三项全部抽中：

- 可用 Turn 只有 Turn 4、Turn 5；
- 保留餐饮和收费娱乐；
- 免费娱乐不生成。

这不是人为设置“每天最多两项行为”，而是该客组当天真实可用时间不足。

NPC 不存在“至少必须做一项”或“固定最多做几项”的人为配额。

---

## 9. 已实现：娱乐执行闭环 Phase 2B.2

当前 `_process_entertainment()` 已改为读取日初 planned action：

- 只执行当前 Turn 到期、且状态为 `pending` 的 `paid_entertainment` / `free_entertainment`；
- 收费娱乐直接读取 action 中固化的 `tier_key`；
- 不再在 Turn 现场 roll，不再重新选档，也不按当前设施等级降档；
- 收费娱乐执行时：
  - `npc.location = "entertainment"`
  - 按档位固定整组收费并增加娱乐收入
  - 增加对应满意度，仍受 100 上限约束
  - 写入包含档位、收入和满意度的事件文本
- 免费娱乐执行时：
  - `npc.location = "entertainment"`
  - 收入 0
  - 满意度 +1
  - 写入免费娱乐事件
- 已完成、失败或跳过的 action 不会重复执行；
- 不新增 `last_entertainment_day` 等顶层状态字段，继续复用 planned action 的 `completed / failed / skipped`。

---

## 10. 当前明确否决的实现方式

以下方向不得采用：

- NPC 到达后才生成计划；
- 到达后再 roll 当天想做什么；
- Turn 现场重新 roll 餐饮或娱乐参与意愿；
- 默认到达 Turn 立即消费；
- 固定所有 NPC 先餐饮、后娱乐；
- 强制每组 NPC 至少进行一项行为；
- 人为规定每组 NPC 固定最多进行几项行为；
- 用 `location` 代替行为意愿；
- 餐饮和娱乐分别维护互不知情的排程；
- 同一 Turn 给同一客组安排多个主要行为；
- 为三个标签制作 27 种组合脚本；
- 使用“基础概率 × 倍率”增加无意义公式层级；
- 用复杂路径规划模拟 NPC 移动；
- 因测试方便提前按容量截断需求；
- 为当前功能顺手扩建大状态机。

---

## 11. 当前开发进度

### 已完成

- Phase 1：全局日初客流与到达计划；
- Phase 2A：餐饮 planned action 与执行闭环；
- Phase 2A.1：餐饮随机 Turn；
- 共同随机排程底座；
- Phase 2B.1：收费娱乐和免费娱乐的日初计划生成。

### 下一阶段

#### Phase 2B.2：娱乐执行闭环

已完成：

- 收费娱乐 action 的执行；
- 免费娱乐 action 的执行；
- 收费娱乐消费档位与价格；
- 娱乐满意度；
- 免费娱乐效果；
- 世界内事件；
- `completed / failed / skipped`；
- 防重复结算；
- `npc.location`；
- 移除 Turn 现场娱乐 roll。

#### 后续

- `temperament` 接入失败反应；
- 预约标签分布与自然客统一；
- 帐篷付费解锁；
- 小费与维修金币；
- 前端与 MCP 状态同步；
- 完整经济平衡。

---

### 11.1 游玩周期与终局目标

- 正常频率玩家预计在现实 3～5 天内，基本体验完温泉之外的主体内容；
- 高频玩家可能在 2～3 天内推进完成，不做人为限速；
- 开发侧主要控制内容开放节奏、无效回合、重复信息、MCP/API 调用与 Token 浪费；
- 不强制规定玩家每天必须玩多少次；
- 温泉建成是 v1 的明确最终通关目标；
- 温泉建成后不强制结束存档，玩家仍可继续经营完全体营地；
- 温泉之外的主体内容应在终局前基本开放，避免玩家为了温泉只剩重复攒钱；
- 后续版本可以继续扩展温泉升级、特色汤池或相关活动，但不属于 v1 当前范围。

### 11.2 温泉定位

- 温泉是独立大型终局设施，不是餐饮、娱乐或绿化的普通升级等级；
- 当前仓库里的地图坐标和图标只是占位，正式经营闭环尚未实现；
- 温泉建成后成为客组计划包中的附加消费行为，按人数收费，收费金额为单价 × group_size；
- 满意度按整组增加一次，使用现有 `total_satisfaction`，不新增温泉口碑、温泉好感或独立评分系统；
- 温泉价格、满意度收益和参与概率应高于普通消费设施，但具体数值尚未确定；
- 温泉应有按人数计算的接待容量，以体现大型高价设施的稀缺性；
- v1 暂时只做一个温泉项目，不拆档位、不做升级线。

---

## 12. 仍未定案或仍待平衡

以下内容仍属于待定事项：

- 温泉附加预约概率；
- 温泉接待人数上限；
- 温泉单人价格；
- 温泉整组满意度收益；
- 温泉建造费用；
- 帐篷解锁正式价格；
- 温泉建成所需的其他前置条件；
- 主体内容对应的具体游戏内经营日数量。

以下旧项已经在本版确认，不再作为待定事项：

- 全局需求档案的具体公式；
- 自然日间需求、自然过夜需求和预约请求的具体概率；
- 日间预约与过夜预约的类型权重；
- 客组人数分布；
- 帐篷容量；

除此之外，后续若要继续做无 API 模拟，还需要继续观察：

- NPC 行为是否出现过度偏向；
- 预约与自然需求的整体占比；
- 温泉加入后是否挤压日常消费；
- 收费娱乐与免费娱乐是否过度集中；
- 不同消费习惯和经济层级之间的差异是否足够明显；
- 是否存在明显的重复回合浪费或过于同质化的计划包。

## 13. 测试与技术状态

当前专项测试：

- `tests.test_entertainment_planned_actions_phase2b`
  - 13 条通过
- `tests.test_dining_planned_actions_phase2a`
  - 16 条通过
- `tests.test_arrival_plan_phase1`
  - 5 条通过

已确认：

- 三项参与判定独立 roll；
- 所有新增 roll 只发生在日初；
- Turn 3 到达且三项全中时，三项可排入 Turn 3～5；
- Turn 4 到达且三项全中时，保留两项收费行为；
- 同一天重复确保计划时不重新 roll；
- 预约客和自然客共用计划生成链；
- 新测试未发现编码污染。

已知环境问题：

- Windows 下 `TemporaryDirectory.cleanup()` 偶发 `PermissionError [WinError 5]`；
- 该问题发生在测试清理阶段，不属于业务断言失败；
- 暂不在 NPC 行为开发中顺手处理。

---

## 14. 本版结论

`v0.6` 已将 NPC 行为系统推进到：

> 日初生成全部 NPC
> → 日初生成 `daily_demand_profile`
> → 日初确定 `arrival_turn`  
> → 需求层分流自然日间需求、自然过夜需求和预约事件
> → 日初独立判定餐饮、收费娱乐、免费娱乐  
> → 根据 `arrival_turn～Turn 5` 的预先可知范围完成共同排程  
> → 将完整结果写入计划包  
> → Turn 只读取并执行

当前已经完成“NPC 今天想做什么、什么时候做、今天会不会多出一条预约事件”的计划层。

下一步只处理“到了对应 Turn 后，娱乐行为如何正确执行和结算”。

后续不得重新引入到达后排程、Turn 现场 roll、固定行为配额或多层概率套壳。
