# 《露营广场》项目规则

## 一、项目定位

《露营广场》是一个 MCP-first 的 AI 经营游戏。

- AI 玩家负责观察状态、制定经营决策和提交操作。
- 游戏代码负责规则判断、状态推进、收费、结算、持久化和错误拦截。
- 人类网页主要用于查看游戏状态、调试和必要的人类操作，不是主要决策端。
- 核心原则：AI 负责决策，代码负责裁决。

不得把关键业务规则交给模型自行理解或临场补全。能够由代码确定的内容，应由代码明确处理。

## 二、正式规则来源

仓库根部的正式策划文档是游戏规则的权威来源：

`露营广场策划.md`

正式策划版本以该文件当前正文为准；旧版本文档仅供历史追溯。

代码、测试、API 和前端应与正式策划保持一致。

- 不得用临时代码中的旧数值反向覆盖正式策划。
- 若代码、测试和正式策划存在冲突，应先报告冲突，不得擅自猜测。
- 尚未定案的规则不得由 Codex 自行补充或发明。

## 三、项目已正式上线

本项目已经正式上线，并存在真实玩家存档。

涉及状态结构、持久化、存档字段、session、数据库 schema、游戏进度或其他可能影响现有存档的修改时，必须先评估线上存档兼容性和数据风险。

除非用户明确确认可以破坏现有存档，否则：

- 不得默认现有玩家存档可以失效；
- 不得擅自删除仍被线上存档使用的正式字段；
- 修改状态结构时，应先判断是否需要兼容、迁移或安全 fallback；
- 涉及数据库 schema、runtime snapshot、session 或持久化格式的改动，应明确说明对现有存档的影响；
- 仅历史开发残留、从未进入正式线上状态的数据或已确认无引用的死代码，可在核实后清理。

不要为了兼容不存在的历史版本无限增加兼容层；只保护当前正式线上版本真实存在的玩家数据和必要兼容路径。

## 四、交付边界

本仓库最终必须达到：

“外部维护者拉取后，可以直接进行 MCP 外层封装和游戏站接入。”

南杉老师只负责：

- 从 GitHub 拉取仓库；
- 编写或套用外层 MCP 封装；
- 接入并登录游戏站。

本仓库内部必须自行完成：

- 全部游戏业务逻辑；
- 完整、稳定、可调用的 API；
- 清晰的 action 名称和参数结构；
- 稳定、简洁的返回结构与错误码；
- 状态查询和可执行操作输出；
- 成长项目等全部正式机制的调用入口；
- 持久化、原子性、幂等和重复调用保护；
- 必要的测试、接口说明和最小调用示例；
- 旧接口、重复机制、死代码和开发残留清理。

不得把以下工作留给 MCP 外层封装者：

- 补业务接口；
- 猜测参数；
- 修复返回结构；
- 补状态字段；
- 处理业务规则；
- 清理旧机制；
- 修复仓库内部测试或持久化问题。

本仓库不负责南杉老师所使用的最终 MCP 外层包装代码，但必须把包装内部的产品完整做好。

## 五、开发工作流

每次任务遵循以下顺序：

1. 先只读检查真实代码、测试、Git 状态和相关正式文档。
2. 用普通语言说明问题、修改范围、不会修改的内容和风险。
3. 优先单点、小步、可验证、可回退的修改。
4. 不得顺手修改任务范围之外的问题。
5. 修改后先运行最小定向测试。
6. 只有用户明确要求时才执行提交。
7. 提交前必须确认实际修改文件和 diff。
8. 提交后必须报告 commit、修改文件、测试结果、工作区状态和 stash 状态。

## 六、Git 安全规则

除非用户明确要求，否则禁止：

- `git commit`
- `git amend`
- `git reset`
- `git checkout`
- `git restore`
- `git stash`
- 应用、删除、覆盖或重建已有 stash
- 推送远程仓库

不得触碰与当前任务无关的未提交文件。

发现额外修改、异常文件或范围外 diff 时，应停止并报告。

## 七、测试环境

项目指定 Python：

`C:\Users\NewAdmin\Desktop\露营\camping_plaza_test_env\py312-20260726\Scripts\python.exe`

- 优先使用指定 Python。
- 不得因路径错误自行安装依赖、创建新环境或换用未知系统 Python。
- 优先运行与当前修改相关的定向测试。
- 只有用户明确要求时才运行全量测试。
- 测试失败时先判断是业务失败、测试假设错误还是环境问题，不得顺手修复无关失败。
- Windows 临时目录清理产生的既有 `PermissionError` 应如实报告，不得自动扩大修改范围。

## 八、接口与输出原则

- API 应提供完整业务入口，MCP 外层不负责补业务逻辑。
- action 名称、参数、错误码和返回结构必须稳定、明确。
- 可执行操作应携带完整参数，不要求外层猜测。
- 状态输出应足够支持 AI 决策，同时避免无意义冗余。
- 失败操作不得产生部分扣款、部分升级或其他半完成状态。
- 重复调用不得重复收费、重复结算或重复累计成长进度。
- 删除正式机制时，应同步移除其 action、状态字段、输出、测试和前端入口。

## 九、机制边界

必须保留当前正式分层：

- 帐篷通过成长项目按顺序解锁；
- 餐饮、娱乐和绿化使用设施等级；
- 帐篷没有单独升级等级；
- 日间营位、住宿、餐饮、收费娱乐分别独立收费；
- 成长项目由经营条件和金币共同控制；
- AI 提交决策，代码执行并验收。

不得重新引入：

- `upgrade_tent`
- 帐篷专属 `level`
- `TENT_UPGRADE_COST`
- `satisfaction_bonus`
- 与正式成长项目重复的旧升级入口

## 十、修改原则

- 不进行无关重构。
- 不为“以后可能需要”提前增加复杂兼容层。
- 不凭空增加新机制、字段或抽象。
- 不通过删除测试掩盖业务问题。
- 测试应验证正式规则，关键数值至少保留一处独立明确断言。
- 能引用权威常量的计算测试应避免重复硬编码。
- 代码、测试、schema、API 和前端必须同步，不能只改其中一层。
